<UserControl x:Class="Costing.UserControls.StaffCostsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Costing.Viewmodels"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="280"
             Background="#1E293B"
             Loaded="UserControl_Loaded">

    <UserControl.DataContext>
        <vm:StaffCostsViewModel/>
    </UserControl.DataContext>

    <Grid>
        <StackPanel Margin="20">

            <Button Content="Back to Menu" Click="btnBack_Click" Margin="0,0,0,30" FontSize="13" FontWeight="SemiBold" 
                    Background="Transparent" BorderThickness="0" Padding="0" HorizontalAlignment="Left">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <TextBlock Text="Staff Costs" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White" Margin="0,0,0,20"/>

            <TextBlock Text="Select Category" Foreground="#CBD5E1" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding OCStaffCosts}" 
                      SelectedItem="{Binding SelectedStaffCost}" 
                      DisplayMemberPath="Category" 
                      Height="32" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="5,0"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Type: " Foreground="#94A3B8"/>
                <TextBlock Text="{Binding SelectedStaffCost.Type}" FontWeight="Bold" Foreground="White"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Value: " Foreground="#94A3B8"/>
                <TextBlock Text="{Binding SelectedStaffCost.Value, StringFormat={}{0:0.#####}}" FontWeight="Bold" Foreground="White"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Base Category: " Foreground="#94A3B8"/>
                <TextBlock Text="{Binding SelectedStaffCost.BaseCategory}" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" MaxWidth="150"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Max Limit: " Foreground="#94A3B8"/>
                <TextBlock Text="{Binding SelectedStaffCost.MaxLimit, StringFormat={}{0:0.00}}" FontWeight="Bold" Foreground="White"/>
            </StackPanel>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Add" Click="btnAdd_Click" Background="Transparent" BorderBrush="#94A3B8" BorderThickness="1" Foreground="#94A3B8" Padding="0,5"/>
                <Button Grid.Column="2" Content="Edit" Click="btnEdit_Click" Background="Transparent" BorderBrush="#3B82F6" BorderThickness="1" Foreground="#3B82F6" Padding="0,5"/>
                <Button Grid.Column="4" Content="Delete" Click="btnDelete_Click" Background="Transparent" BorderBrush="#EF4444" BorderThickness="1" Foreground="#EF4444" Padding="0,5"/>
                <Rectangle Width="1" Fill="#CBD5E1" Margin="0,5,20,5" />
                
            </Grid>


            <Border x:Name="FormPanel" Visibility="Collapsed" Background="#0F172A" CornerRadius="6" Padding="15" Margin="0,10,0,0">
                <StackPanel>
                    <TextBlock x:Name="txtFormTitle" Text="Edit Category" Foreground="White" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                    <TextBlock Text="Category Name" Foreground="#CBD5E1" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFormCategory" Height="32" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,15"/>

                    <TextBlock Text="Cost Type" Foreground="#CBD5E1" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbFormType" ItemsSource="{Binding TypesList}" Height="32" Margin="0,0,0,15" 
                              VerticalContentAlignment="Center" Padding="5,0" SelectionChanged="cmbFormType_SelectionChanged"/>

                    <TextBlock Text="Base Category (If %):" Foreground="#CBD5E1" FontWeight="SemiBold" Margin="0,0,0,5"/>

                    <Border CornerRadius="4" Padding="8" Margin="0,0,0,15">
                        <WrapPanel x:Name="pnlBaseCategories" Orientation="Horizontal" />
                    </Border>

                    <TextBlock Text="Cost Value" Foreground="#CBD5E1" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFormValue" Height="32" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,0"/>

                    <StackPanel Margin="0,15,0,0">
                        <TextBlock Text="Max Annual Limit (Optional)" FontWeight="Medium" Foreground="#CBD5E1" Margin="0,0,0,0"/>
                        <TextBox x:Name="txtFormMaxLimit" Height="35" VerticalContentAlignment="Center"/>
                        <TextBlock Text="Leave blank if there is no cap." FontSize="11" Foreground="#94A3B8" Margin="0,2,0,10"/>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Cancel" Click="btnCancelForm_Click" Background="Transparent" BorderBrush="#94A3B8" BorderThickness="1" Foreground="#94A3B8" Padding="0,5"/>
                        <Button Grid.Column="2" Content="Confirm" Click="btnConfirmForm_Click" Background="#3B82F6" BorderThickness="0" Foreground="White" Padding="0,5"/>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>

        
    </Grid>
</UserControl>