<Window x:Class="Costing.UserControls.AllocationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assign Work Centres"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="620" Height="500"
        Background="#F8FAFC">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="Employee: " FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock x:Name="tbEmployeeName" FontSize="18" FontWeight="Bold" Foreground="#3B82F6"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="Add work centre assignments below. Portions must add up to exactly 1.00 (100%). A new row is added automatically while the total is below 1."
                   TextWrapping="Wrap"
                   Foreground="#64748B"
                   FontSize="12"
                   Margin="0,0,0,14"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="36"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Work Centre"       FontWeight="SemiBold" Foreground="#374151" Margin="4,0,0,0"/>
                <TextBlock Grid.Column="1" Text="Allocation (0–1)"  FontWeight="SemiBold" Foreground="#374151" Margin="4,0,0,0"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="icRows">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,3,0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="36"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox Grid.Column="0"
                                          ItemsSource="{Binding DataContext.WorkCentres, RelativeSource={RelativeSource AncestorType=Window}}"
                                          DisplayMemberPath="WcDescription"
                                          SelectedValuePath="WcCode"
                                          SelectedValue="{Binding WorkCentre, UpdateSourceTrigger=PropertyChanged}"
                                          Margin="0,0,8,0"
                                          Height="32"
                                          Padding="6,4"/>

                                <TextBox Grid.Column="1"
                                         Text="{Binding Portion, StringFormat={}{0:0.##}}"
                                         Height="32"
                                         Padding="6,4"
                                         Margin="0,0,8,0"
                                         TextChanged="TbPortion_TextChanged"
                                         PreviewTextInput="TbPortion_PreviewTextInput"
                                         Tag="{Binding}"/>

                                <Button Grid.Column="2"
                                        Content="✕"
                                        Width="28" Height="28"
                                        Background="#FEE2E2"
                                        Foreground="#DC2626"
                                        BorderThickness="0"
                                        FontSize="11"
                                        Cursor="Hand"
                                        Tag="{Binding}"
                                        Click="BtnRemoveRow_Click"
                                        ToolTip="Remove this row"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="3"
                Background="#F1F5F9"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8"
                Margin="0,12,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Total Allocation:" FontWeight="SemiBold" Foreground="#374151" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="tbTotal" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                    <TextBlock x:Name="tbTotalWarning" Text="" Foreground="#DC2626" FontSize="12" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="90" Height="36"
                    Margin="0,0,10,0"
                    Background="#F1F5F9"
                    Foreground="#374151"
                    BorderBrush="#CBD5E1"
                    BorderThickness="1"
                    Cursor="Hand"
                    Click="BtnCancel_Click"/>
            <Button x:Name="btnSave"
                    Content="Save"
                    Width="90" Height="36"
                    Background="#3B82F6"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="Bold"
                    Cursor="Hand"
                    Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
