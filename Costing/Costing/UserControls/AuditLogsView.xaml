<UserControl x:Class="Costing.UserControls.AuditLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Costing.Viewmodels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="White"
             Loaded="UserControl_Loaded">

    <UserControl.DataContext>
        <vm:AuditLogsViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="ModernHeaderButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="10,0">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15" Direction="270" Color="Black"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Header Area -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Audit Logs" FontSize="24" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center" Margin="0,0,30,0"/>
            <TextBlock Grid.Column="1" Text="Select Snapshot:" Foreground="#64748B" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>

            <ComboBox x:Name="cmbSnapshots" Grid.Column="2" Height="36" VerticalContentAlignment="Center" Padding="10,0" SelectionChanged="cmbSnapshots_SelectionChanged"/>

            <!-- Action Buttons -->
            <Button x:Name="btnDelete" Grid.Column="4" Content="Delete Snapshot" Click="btnDelete_Click" Style="{StaticResource ModernHeaderButton}" Background="#EF4444" Width="140" Height="36"/>
            <Button x:Name="btnExport" Grid.Column="6" Content="Export to Excel" Click="btnExport_Click" Style="{StaticResource ModernHeaderButton}" Background="#10B981" Width="140" Height="36"/>
        </Grid>

        <!-- Row 1: System Rates Info Bar -->
        <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="15,10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Snapshot Calculation Rates:" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <TextBlock Foreground="#3B82F6" FontWeight="SemiBold" Margin="0,0,20,0" VerticalAlignment="Center">
                    <Run Text="Bonus: "/><Run Text="{Binding SelectedSnapshot.BonusRate, StringFormat={}{0:0.#####}}"/>
                </TextBlock>
                <TextBlock Foreground="#3B82F6" FontWeight="SemiBold" Margin="0,0,20,0" VerticalAlignment="Center">
                    <Run Text="UIF: "/><Run Text="{Binding SelectedSnapshot.UifRate, StringFormat={}{0:0.#####}}"/>
                </TextBlock>
                <TextBlock Foreground="#3B82F6" FontWeight="SemiBold" VerticalAlignment="Center">
                    <Run Text="SDL: "/><Run Text="{Binding SelectedSnapshot.SdlRate, StringFormat={}{0:0.#####}}"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Row 2: Read-Only Data Grid -->
        <DataGrid x:Name="dgAuditLogs" Grid.Row="2" 
                  ItemsSource="{Binding OCAuditLogs}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#E2E8F0"
                  HorizontalGridLinesBrush="#E2E8F0"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="#F8FAFC">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="80" FontWeight="SemiBold" Foreground="#475569"/>
                <DataGridTextColumn Header="Employee Name" Binding="{Binding EmployeeName}" Width="200"/>
                <DataGridTextColumn Header="Snapshot Date" Binding="{Binding SnapshotDate, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="150" Foreground="#64748B"/>

                <DataGridTextColumn Header="Bonus" Binding="{Binding Bonus, StringFormat={}{0:N2}}" Width="120" Foreground="#2563EB"/>
                <DataGridTextColumn Header="UIF" Binding="{Binding UIF, StringFormat={}{0:N2}}" Width="120" Foreground="#2563EB"/>
                <DataGridTextColumn Header="SDL" Binding="{Binding SDL, StringFormat={}{0:N2}}" Width="120" Foreground="#2563EB"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>