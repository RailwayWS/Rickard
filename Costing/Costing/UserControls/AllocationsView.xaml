<UserControl x:Class="Costing.UserControls.AllocationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8FAFC">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        

        <TextBlock Text="Staff Work Centre Allocations" FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>

        

        <DataGrid Grid.Row="1" x:Name="dgAllocations" AutoGenerateColumns="False" 
                  ItemsSource="{Binding OCAllocations}" 
                  CanUserAddRows="False"
                  Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee Code" Binding="{Binding Code}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Employee Name" Binding="{Binding Name}" Width="250" IsReadOnly="True"/>
                <DataGridTextColumn Header="Cost Centre" Binding="{Binding CostCentre}" Width="120" IsReadOnly="True" Foreground="#64748B"/>

                <DataGridTemplateColumn Header="Assigned Work Centre" Width="300">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.OCWorkCentres, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="WcDescription" 
                                      SelectedValuePath="WcCode"
                                      SelectedValue="{Binding WorkCentre, UpdateSourceTrigger=PropertyChanged}"
                                      Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" x:Name="btnSave" Content="Save Allocations" Click="btnSave_Click" Width="150" Height="40" HorizontalAlignment="Right" Margin="0,20,0,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
    </Grid>
</UserControl>